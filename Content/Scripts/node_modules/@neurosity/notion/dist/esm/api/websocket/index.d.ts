import { Subscription } from "../../types/subscriptions";
/**
 * @hidden
 */
declare type WebsocketOptions = {
    deviceId: string;
    socketUrl?: string;
    secure?: boolean;
};
/**
 * @hidden
 */
export declare class WebsocketClient {
    static serverType: string;
    private options;
    protected socket: any;
    constructor(options: WebsocketOptions);
    onMetric(subscription: Subscription, callback: Function): Function;
    removeMetricListener(subscription: Subscription, listener: Function): void;
    disconnect(): void;
}
export {};
